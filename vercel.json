{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "build": {
    "env": {
      "GO_BUILD_FLAGS": "-ldflags '-s -w'"
    }
  },
  "crons": [
    {
      "path": "/cron",
      "schedule": "*/5 * * * *"
    }
  ],
  "routes": [
    { "src": "(?:.*)", "dest": "api/index.go" }
  ]
}